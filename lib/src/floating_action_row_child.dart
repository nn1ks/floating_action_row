import 'package:flutter/material.dart';

class FloatingActionRowChild extends StatelessWidget {
  final Color color;
  final EdgeInsets padding;
  final bool isButton;

  FloatingActionRowChild({
    this.color,
    this.padding,
    this.isButton,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
